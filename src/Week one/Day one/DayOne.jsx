import React from "react";

const DayOne = () => {
  return (
    <div>
      <h1>Day 1: Introduction to React and Setup Development Environment</h1>
      {/*
      What is React?

React: A JavaScript library for building user interfaces, developed by Facebook.
Core Concepts:

Components: Building blocks of a React application.
JSX: JavaScript XML, a syntax extension for writing HTML in JavaScript.
State and Props: Mechanisms for managing data in components.
Advantages:

Component-based architecture for reusable and maintainable code.
Virtual DOM for efficient updates.
Rich ecosystem and community support.
Setting Up the Development Environment:

Install Node.js and npm:
Go to the Node.js website and download the LTS version.
Run the installer and follow the instructions.
Verify the installation by opening a terminal (Command Prompt, PowerShell, or Terminal) and typing:
node -v
npm -v
Create a New React Project:
Open a terminal and run the following command to install Create React App globally
npm install -g create-react-app
Understanding the Project Structure:

Project Files:
public/: Contains the public assets of the project.
src/: Contains the source code of the project.
src/index.js: Entry point for the React application.
src/App.js: Main application component.

Introduction to JSX:

JSX: A syntax extension for JavaScript that looks similar to HTML.
JSX in React:
Allows you to write HTML elements in JavaScript and place them in the DOM without using createElement() or appendChild().
const element = <h1>Hello, world!</h1>;

      */}
    </div>
  );
};

export default DayOne;
